---
description: Run the full build (client + server)
---

Run the full production build pipeline.

```
!`pnpm build`
```

If the build fails, analyze the errors (typically TypeScript type errors) and suggest fixes.
