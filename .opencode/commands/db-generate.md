---
description: Generate a new migration from schema changes
---

Generate a new Drizzle ORM migration based on schema changes in `hono/db/schema.ts`.

```
!`pnpm db:generate`
```

Report what migration was generated. Remind the user to review the generated SQL before applying.
