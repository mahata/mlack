---
description: Run Biome lint and format check
---

Run the Biome linter and format checker.

```
!`pnpm lint $ARGUMENTS`
```

If there are lint errors, summarize them grouped by rule. Suggest running `/lint-fix` to auto-fix, or explain manual fixes needed.
