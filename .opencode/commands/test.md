---
description: Run unit tests (Vitest)
---

Run the unit tests using Vitest. If arguments are provided, pass them through.

```
!`pnpm test:run $ARGUMENTS`
```

If any tests fail, analyze the failures and suggest fixes. Do not auto-fix unless explicitly asked.
